export const FLAGS = {
  // Hide dev/testing pages in production
  ENABLE_OPENAI_TEST: false,
  // Toggle Weekly Planner availability (currently not linked in main nav)
  ENABLE_WEEKLY_PLANNER: false,
} as const;
